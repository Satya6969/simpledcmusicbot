services:
  discord-music-bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    env_file:
      - .env
    # Optional: mount a local cookies file directory for YouTube auth challenges.
    volumes:
      - ./cookies:/cookies:ro
